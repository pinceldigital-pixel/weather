Fix Pack (carga de datos)
1) Reemplazá tu app.js y sw.js por estos.
2) Hard reload: DevTools → Recargar (vaciar caché).
3) Serví por HTTPS o localhost.
Si OpenWeather falla (cuota/key), se usa Open-Meteo como respaldo.
